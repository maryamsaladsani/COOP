# Base URL of the COOP backend API (see ../Backend). CRA only embeds vars
# prefixed with REACT_APP_ at build time, and requires a dev-server restart
# to pick up changes.
REACT_APP_API_URL=http://localhost:3001
